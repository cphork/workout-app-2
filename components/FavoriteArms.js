const FavoriteArms = () => {

    const [faveWorkout, setFaveWorkout] = useState([])

    // const Favorites = faveWorkout.map((fave, index) => {
    //     return (
    //         <h1>Filler</h1>
    //         // { fave.title }
    //     )
    // })
    return (
        <h1>Favorite Arms</h1>
        // { Favorites }
    );
}

export default FavoriteArms;